language: node_js
node_js:
  - stable
cache:
  directories:
    - node_modules
script:
  - npm install -g @vue/cli
  - npm install
  - npm run test:unit
on:
  branch: Test
deploy:
  provider: surge
  project: ./dist
  domain: iaas-frontend.surge.sh
  cleanup: false
